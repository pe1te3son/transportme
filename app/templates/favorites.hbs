<div class="favorites-wrapper">
  <div class="row">
    <div class="col l6 s12">

      {{#each model as |fav|}}
        {{fav-head fav=fav}}
      {{/each}}

    </div>

    <div class="col l6 s12">
      {{outlet}}
    </div>

  </div>
</div>
