import Ember from 'ember';

/**
* @name Circle Loader Component
* @desc Circle Loader
* @param { Boolean } isLoading - boolean
*/
export default Ember.Component.extend({
  tagName: 'div',
  classNames: 'circle-loader inline-block'
});
