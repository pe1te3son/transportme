import Ember from 'ember';

/**
* @name Linear Loader Component
* @desc Linear Loader
* @param { Boolean } isLoading - boolean
*/
export default Ember.Component.extend({
  tagName: 'div',
  classNames: 'linear-loader'
});
