import Ember from 'ember';

/**
* @name Route Body Component
* @desc Displays route body ( part that slides down on click  )
* @param { Object } route - journey
*/
export default Ember.Component.extend({
});
