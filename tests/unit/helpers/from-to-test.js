import { fromTo } from 'transportme/helpers/from-to';
import { module, test } from 'qunit';

module('Unit | Helper | from to');

// Replace this with your real tests.
test('it works', function (assert) {
  let result = fromTo([42]);
  assert.ok(result);
});
