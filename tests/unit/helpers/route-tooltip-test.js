import { routeTooltip } from 'transportme/helpers/route-tooltip';
import { module, test } from 'qunit';

module('Unit | Helper | route tooltip');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = routeTooltip([42]);
  assert.ok(result);
});
