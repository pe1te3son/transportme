
import { filterDestinations } from 'transportme/helpers/filter-destinations';
import { module, test } from 'qunit';

module('Unit | Helper | filter destinations');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = filterDestinations([42]);
  assert.ok(result);
});

