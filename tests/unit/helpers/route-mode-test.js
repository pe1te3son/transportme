import { routeMode } from 'transportme/helpers/route-mode';
import { module, test } from 'qunit';

module('Unit | Helper | route mode');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = routeMode([42]);
  assert.ok(result);
});
