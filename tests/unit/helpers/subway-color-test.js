import { subwayColor } from 'transportme/helpers/subway-color';
import { module, test } from 'qunit';

module('Unit | Helper | subway color');

// Replace this with your real tests.
test('it works', function (assert) {
  let result = subwayColor([42]);
  assert.ok(result);
});
