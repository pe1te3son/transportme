import { acknowledgements } from 'transportme/helpers/acknowledgements';
import { module, test } from 'qunit';

module('Unit | Helper | acknowledgements');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = acknowledgements([42]);
  assert.ok(result);
});
