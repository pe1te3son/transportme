import { uniqList } from 'transportme/helpers/uniq-list';
import { module, test } from 'qunit';

module('Unit | Helper | uniq list');

// Replace this with your real tests.
test('it works', function (assert) {
  let result = uniqList([42]);
  assert.ok(result);
});
