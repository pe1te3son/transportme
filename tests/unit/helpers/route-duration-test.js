import { routeDuration } from 'transportme/helpers/route-duration';
import { module, test } from 'qunit';

module('Unit | Helper | route duration');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = routeDuration([42]);
  assert.ok(result);
});
