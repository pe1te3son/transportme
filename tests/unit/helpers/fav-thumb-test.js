import { favThumb } from 'transportme/helpers/fav-thumb';
import { module, test } from 'qunit';

module('Unit | Helper | fav thumb');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = favThumb([42]);
  assert.ok(result);
});
